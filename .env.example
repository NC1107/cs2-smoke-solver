# Required
SMOKESOLVER_DOMAIN=smokesolver.example.com

# Optional: override to build and use a local image instead of the published
# ghcr.io one (e.g. `docker compose up --build` with no ghcr access)
# SMOKESOLVER_IMAGE=smokesolver-serve:latest
